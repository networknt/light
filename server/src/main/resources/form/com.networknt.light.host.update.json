{
  "formId": "com.networknt.light.host.update",
  "version": 1,
  "action" : [{
    "category" : "host",
    "name" : "updHost",
    "readOnly": false,
    "title": "Submit",
    "success" : "/admin/hostAdmin"
  }],
  "schema": {
    "type": "object",
    "title": "Host",
    "required": ["hostId", "base", "transferMinSize"],
    "properties": {
      "hostId": {
        "title": "Host Id",
        "type": "string",
        "readOnly": true
      },
      "base": {
        "title": "Base",
        "type": "string"
      },
      "transferMinSize": {
        "title": "Transfer Min Size",
        "type": "string"
      }
    }
  },
  "form": [
    "hostId",
    "base",
    "transferMinSize"
  ]
}
